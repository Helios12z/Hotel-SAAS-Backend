MỨC ƯU TIÊN CAO (Core Business Flows)

  1️⃣ LUỒNG ĐẶT PHÒNG (BOOKING FLOW) - Quan trọng nhất
  ┌──────┬───────────────────────────────────┬─────────────────────────────┐
  │ Bước │           API Endpoint            │          Nghiệp vụ          │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 1    │ GET /api/hotels/search            │ Tìm kiếm khách sạn          │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 2    │ GET /api/hotels/{id}/availability │ Kiểm tra phòng trống        │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 3    │ POST /api/bookings/calculate      │ Tính giá đặt phòng          │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 4    │ POST /api/bookings                │ Tạo booking                 │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 5    │ POST /api/checkout/booking        │ Tạo checkout session Stripe │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 6    │ POST /api/checkout/webhook        │ Webhook xử lý thanh toán    │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 7    │ POST /api/bookings/{id}/confirm   │ Xác nhận booking            │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 8    │ POST /api/bookings/{id}/checkin   │ Check-in khách              │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 9    │ POST /api/bookings/charges        │ Thêm phí dịch vụ            │
  ├──────┼───────────────────────────────────┼─────────────────────────────┤
  │ 10   │ POST /api/bookings/{id}/checkout  │ Check-out khách             │
  └──────┴───────────────────────────────────┴─────────────────────────────┘
  Test cases: Tạo booking → Thanh toán thành công → Check-in → Thêm charge → Check-out

  ---
  2️⃣ LUỒNG ĐĂNG KÝ KHÁCH SẠN (HOTEL ONBOARDING)
  ┌──────┬─────────────────────────────────────────┬────────────────────────────────────────────┐
  │ Bước │              API Endpoint               │                 Nghiệp vụ                  │
  ├──────┼─────────────────────────────────────────┼────────────────────────────────────────────┤
  │ 1    │ POST /api/onboarding                    │ Tạo đơn đăng ký                            │
  ├──────┼─────────────────────────────────────────┼────────────────────────────────────────────┤
  │ 2    │ POST /api/onboarding/{id}/documents     │ Upload tài liệu                            │
  ├──────┼─────────────────────────────────────────┼────────────────────────────────────────────┤
  │ 3    │ POST /api/onboarding/{id}/submit        │ Gửi đơn                                    │
  ├──────┼─────────────────────────────────────────┼────────────────────────────────────────────┤
  │ 4    │ GET /api/onboarding/admin/pending       │ Admin xem đơn chờ                          │
  ├──────┼─────────────────────────────────────────┼────────────────────────────────────────────┤
  │ 5    │ POST /api/onboarding/admin/{id}/review  │ Review đơn                                 │
  ├──────┼─────────────────────────────────────────┼────────────────────────────────────────────┤
  │ 6    │ POST /api/onboarding/admin/{id}/approve │ Phê duyệt (tạo Brand, Hotel, Subscription) │
  └──────┴─────────────────────────────────────────┴────────────────────────────────────────────┘
  Test cases: Tạo đơn → Upload docs → Submit → Admin approve → Verify tạo Brand/Hotel

  ---
  3️⃣ LUỒNG THANH TOÁN (PAYMENT FLOW)
  ┌──────┬───────────────────────────────────────────────┬─────────────────────────────────────┐
  │ Bước │                 API Endpoint                  │              Nghiệp vụ              │
  ├──────┼───────────────────────────────────────────────┼─────────────────────────────────────┤
  │ 1    │ POST /api/checkout/booking                    │ Tạo session thanh toán booking      │
  ├──────┼───────────────────────────────────────────────┼─────────────────────────────────────┤
  │ 2    │ POST /api/checkout/subscription               │ Tạo session thanh toán subscription │
  ├──────┼───────────────────────────────────────────────┼─────────────────────────────────────┤
  │ 3    │ GET /api/checkout/session/{id}                │ Kiểm tra trạng thái                 │
  ├──────┼───────────────────────────────────────────────┼─────────────────────────────────────┤
  │ 4    │ POST /api/checkout/webhook                    │ Webhook xử lý                       │
  ├──────┼───────────────────────────────────────────────┼─────────────────────────────────────┤
  │ 5    │ POST /api/bookings/{id}/payments/{pid}/refund │ Hoàn tiền                           │
  └──────┴───────────────────────────────────────────────┴─────────────────────────────────────┘
  Test cases: Tạo session → Simulate webhook checkout.session.completed → Verify booking confirmed

  ---
  4️⃣ LUỒNG QUẢN LÝ BOOKING (CHECK-IN/CHECK-OUT)
  ┌──────┬─────────────────────────────────────────────────┬────────────────────────┐
  │ Bước │                  API Endpoint                   │       Nghiệp vụ        │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 1    │ GET /api/bookings/{id}                          │ Xem chi tiết booking   │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 2    │ POST /api/bookings/{id}/confirm                 │ Xác nhận booking       │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 3    │ POST /api/bookings/{id}/checkin                 │ Check-in               │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 4    │ POST /api/bookings/{id}/change-room             │ Đổi phòng              │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 5    │ POST /api/bookings/{id}/late-checkout/calculate │ Tính phí late checkout │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 6    │ POST /api/bookings/{id}/late-checkout           │ Late checkout          │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 7    │ GET /api/bookings/{id}/charges                  │ Xem các khoản phí      │
  ├──────┼─────────────────────────────────────────────────┼────────────────────────┤
  │ 8    │ POST /api/bookings/{id}/checkout                │ Check-out              │
  └──────┴─────────────────────────────────────────────────┴────────────────────────┘
  Test cases: Confirm → Check-in → Add charges → Check-out → Verify room status = Cleaning

  ---
  MỨC ƯU TIÊN TRUNG BÌNH

  5️⃣ LUỒNG QUẢN LÝ SUBSCRIPTION
  ┌──────┬────────────────────────────────────────────────┬──────────────────┐
  │ Bước │                  API Endpoint                  │    Nghiệp vụ     │
  ├──────┼────────────────────────────────────────────────┼──────────────────┤
  │ 1    │ GET /api/subscription-plans                    │ Xem các gói      │
  ├──────┼────────────────────────────────────────────────┼──────────────────┤
  │ 2    │ POST /api/subscriptions                        │ Tạo subscription │
  ├──────┼────────────────────────────────────────────────┼──────────────────┤
  │ 3    │ POST /api/subscriptions/{id}/change-plan       │ Đổi gói          │
  ├──────┼────────────────────────────────────────────────┼──────────────────┤
  │ 4    │ POST /api/subscriptions/{id}/cancel            │ Hủy subscription │
  ├──────┼────────────────────────────────────────────────┼──────────────────┤
  │ 5    │ POST /api/subscriptions/{id}/renew             │ Gia hạn          │
  ├──────┼────────────────────────────────────────────────┼──────────────────┤
  │ 6    │ GET /api/subscriptions/hotel/{id}/can-add-room │ Check limit      │
  └──────┴────────────────────────────────────────────────┴──────────────────┘
  Test cases: Tạo subscription → Verify hotel limit → Change plan → Cancel

  ---
  6️⃣ LUỒNG QUẢN LÝ KHÁCH SẠN (HOTEL CRUD)
  ┌──────┬──────────────────────────────┬───────────────────┐
  │ Bước │         API Endpoint         │     Nghiệp vụ     │
  ├──────┼──────────────────────────────┼───────────────────┤
  │ 1    │ GET /api/hotels              │ List hotels       │
  ├──────┼──────────────────────────────┼───────────────────┤
  │ 2    │ GET /api/hotels/{id}/details │ Xem chi tiết      │
  ├──────┼──────────────────────────────┼───────────────────┤
  │ 3    │ GET /api/hotels/search       │ Tìm kiếm nâng cao │
  ├──────┼──────────────────────────────┼───────────────────┤
  │ 4    │ POST /api/hotels             │ Tạo hotel         │
  ├──────┼──────────────────────────────┼───────────────────┤
  │ 5    │ PUT /api/hotels/{id}         │ Cập nhật          │
  ├──────┼──────────────────────────────┼───────────────────┤
  │ 6    │ DELETE /api/hotels/{id}      │ Xóa               │
  └──────┴──────────────────────────────┴───────────────────┘
  Test cases: Create → Search với filter → Update → Delete

  ---
  7️⃣ LUỒNG QUẢN LÝ PHÒNG (ROOM MANAGEMENT)
  ┌──────┬───────────────────────────────────────────────┬────────────────┐
  │ Bước │                 API Endpoint                  │   Nghiệp vụ    │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 1    │ GET /api/hotels/{id}/rooms                    │ List phòng     │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 2    │ GET /api/hotels/{id}/rooms/available          │ Phòng trống    │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 3    │ POST /api/hotels/{id}/rooms                   │ Tạo phòng      │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 4    │ PUT /api/hotels/{id}/rooms/{rid}              │ Cập nhật phòng │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 5    │ PATCH /api/hotels/{id}/rooms/{rid}/status     │ Đổi status     │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 6    │ POST /api/hotels/{id}/rooms/{rid}/maintenance │ Báo hỏng       │
  ├──────┼───────────────────────────────────────────────┼────────────────┤
  │ 7    │ PATCH /api/hotels/{id}/rooms/{rid}/available  │ Hoàn tất dọn   │
  └──────┴───────────────────────────────────────────────┴────────────────┘
  Test cases: Create room → Maintenance → Available after cleaning → Delete

  ---
  8️⃣ LUỒNG DASHBOARD & BÁO CÁO
  ┌──────┬─────────────────────────────────────────┬─────────────────┐
  │ Bước │              API Endpoint               │    Nghiệp vụ    │
  ├──────┼─────────────────────────────────────────┼─────────────────┤
  │ 1    │ GET /api/dashboard/hotel/{id}           │ Dashboard hotel │
  ├──────┼─────────────────────────────────────────┼─────────────────┤
  │ 2    │ GET /api/dashboard/hotel/{id}/revenue   │ Doanh thu       │
  ├──────┼─────────────────────────────────────────┼─────────────────┤
  │ 3    │ GET /api/dashboard/hotel/{id}/occupancy │ Tỷ lệ lấp đầy   │
  ├──────┼─────────────────────────────────────────┼─────────────────┤
  │ 4    │ GET /api/reports/revenue/excel          │ Export Excel    │
  ├──────┼─────────────────────────────────────────┼─────────────────┤
  │ 5    │ GET /api/reports/revenue/pdf            │ Export PDF      │
  └──────┴─────────────────────────────────────────┴─────────────────┘
  Test cases: Get dashboard stats → Export revenue report → Verify file

  ---
  MỨC ƯU TIÊN THẤP

  9️⃣ LUỒNG AUTHENTICATION
  ┌──────┬────────────────────────────────┬───────────────┐
  │ Bước │          API Endpoint          │   Nghiệp vụ   │
  ├──────┼────────────────────────────────┼───────────────┤
  │ 1    │ POST /api/auth/register        │ Đăng ký       │
  ├──────┼────────────────────────────────┼───────────────┤
  │ 2    │ POST /api/auth/login           │ Đăng nhập     │
  ├──────┼────────────────────────────────┼───────────────┤
  │ 3    │ POST /api/auth/refresh-token   │ Refresh token │
  ├──────┼────────────────────────────────┼───────────────┤
  │ 4    │ POST /api/auth/change-password │ Đổi mật khẩu  │
  ├──────┼────────────────────────────────┼───────────────┤
  │ 5    │ POST /api/auth/logout          │ Đăng xuất     │
  └──────┴────────────────────────────────┴───────────────┘
  Test cases: Register → Login → Access protected endpoint → Refresh token → Logout

  ---
  🔟 LUỒNG QUẢN LÝ USER
  ┌──────┬────────────────────────┬─────────────────────────┐
  │ Bước │      API Endpoint      │        Nghiệp vụ        │
  ├──────┼────────────────────────┼─────────────────────────┤
  │ 1    │ POST /api/users        │ Tạo user (hierarchical) │
  ├──────┼────────────────────────┼─────────────────────────┤
  │ 2    │ GET /api/users         │ List users              │
  ├──────┼────────────────────────┼─────────────────────────┤
  │ 3    │ GET /api/users/{id}    │ Xem user                │
  ├──────┼────────────────────────┼─────────────────────────┤
  │ 4    │ PUT /api/users/{id}    │ Cập nhật                │
  ├──────┼────────────────────────┼─────────────────────────┤
  │ 5    │ DELETE /api/users/{id} │ Xóa                     │
  └──────┴────────────────────────┴─────────────────────────┘
  Test cases: Create BrandAdmin → Create HotelManager under BrandAdmin → Delete

  ---
  1️⃣1️⃣ LUỒNG PROMOTIONS
  ┌──────┬────────────────────────────────────┬─────────────────┐
  │ Bước │            API Endpoint            │    Nghiệp vụ    │
  ├──────┼────────────────────────────────────┼─────────────────┤
  │ 1    │ POST /api/promotions               │ Tạo promotion   │
  ├──────┼────────────────────────────────────┼─────────────────┤
  │ 2    │ GET /api/promotions                │ List promotions │
  ├──────┼────────────────────────────────────┼─────────────────┤
  │ 3    │ POST /api/promotions/validate      │ Validate coupon │
  ├──────┼────────────────────────────────────┼─────────────────┤
  │ 4    │ POST /api/promotions/{id}/activate │ Kích hoạt       │
  ├──────┼────────────────────────────────────┼─────────────────┤
  │ 5    │ POST /api/promotions/{id}/pause    │ Tạm dừng        │
  └──────┴────────────────────────────────────┴─────────────────┘
  Test cases: Create promo → Validate trong booking → Activate → Pause

  ---
  1️⃣2️⃣ LUỒNG REVIEWS
  ┌──────┬──────────────────────────────────────────────┬──────────────┐
  │ Bước │                 API Endpoint                 │  Nghiệp vụ   │
  ├──────┼──────────────────────────────────────────────┼──────────────┤
  │ 1    │ GET /api/hotels/{id}/reviews                 │ Xem reviews  │
  ├──────┼──────────────────────────────────────────────┼──────────────┤
  │ 2    │ POST /api/hotels/{id}/reviews                │ Tạo review   │
  ├──────┼──────────────────────────────────────────────┼──────────────┤
  │ 3    │ POST /api/hotels/{id}/reviews/{rid}/approve  │ Duyệt review │
  ├──────┼──────────────────────────────────────────────┼──────────────┤
  │ 4    │ POST /api/hotels/{id}/reviews/{rid}/response │ Phản hồi     │
  └──────┴──────────────────────────────────────────────┴──────────────┘
  Test cases: Create review → Approve → Add response → Delete

  ---
  1️⃣3️⃣ LUỒNG GUEST PROFILE
  ┌──────┬────────────────────────────────┬──────────────────────┐
  │ Bước │          API Endpoint          │      Nghiệp vụ       │
  ├──────┼────────────────────────────────┼──────────────────────┤
  │ 1    │ GET /api/guest/profile         │ Xem profile          │
  ├──────┼────────────────────────────────┼──────────────────────┤
  │ 2    │ PUT /api/guest/preferences     │ Cập nhật preferences │
  ├──────┼────────────────────────────────┼──────────────────────┤
  │ 3    │ GET /api/guest/bookings        │ Lịch sử booking      │
  ├──────┼────────────────────────────────┼──────────────────────┤
  │ 4    │ GET /api/guest/recently-viewed │ Đã xem gần đây       │
  └──────┴────────────────────────────────┴──────────────────────┘
  Test cases: Update preferences → View booking history

  ---
  1️⃣4️⃣ LUỒNG WISHLIST
  ┌──────┬─────────────────────────────────────┬───────────────────┐
  │ Bước │            API Endpoint             │     Nghiệp vụ     │
  ├──────┼─────────────────────────────────────┼───────────────────┤
  │ 1    │ POST /api/wishlist                  │ Thêm vào wishlist │
  ├──────┼─────────────────────────────────────┼───────────────────┤
  │ 2    │ GET /api/wishlist                   │ Xem wishlist      │
  ├──────┼─────────────────────────────────────┼───────────────────┤
  │ 3    │ POST /api/wishlist/toggle/{hotelId} │ Toggle            │
  ├──────┼─────────────────────────────────────┼───────────────────┤
  │ 4    │ DELETE /api/wishlist/{hotelId}      │ Xóa               │
  └──────┴─────────────────────────────────────┴───────────────────┘
  Test cases: Add hotel → Check if in wishlist → Remove

  ---
  1️⃣5️⃣ LUỒNG NOTIFICATIONS
  ┌──────┬─────────────────────────────────────┬────────────────────┐
  │ Bước │            API Endpoint             │     Nghiệp vụ      │
  ├──────┼─────────────────────────────────────┼────────────────────┤
  │ 1    │ GET /api/notifications              │ List notifications │
  ├──────┼─────────────────────────────────────┼────────────────────┤
  │ 2    │ GET /api/notifications/unread-count │ Đếm chưa đọc       │
  ├──────┼─────────────────────────────────────┼────────────────────┤
  │ 3    │ POST /api/notifications/{id}/read   │ Đánh dấu đọc       │
  ├──────┼─────────────────────────────────────┼────────────────────┤
  │ 4    │ POST /api/notifications/read-all    │ Đọc tất cả         │
  └──────┴─────────────────────────────────────┴────────────────────┘
  Test cases: Get unread → Mark one as read → Read all

  ---
  1️⃣6️⃣ LUỒNG AI CHATBOT (RAG)
  ┌──────┬─────────────────────────────────┬────────────────────┐
  │ Bước │          API Endpoint           │     Nghiệp vụ      │
  ├──────┼─────────────────────────────────┼────────────────────┤
  │ 1    │ POST /api/bot/conversations     │ Tạo conversation   │
  ├──────┼─────────────────────────────────┼────────────────────┤
  │ 2    │ POST /api/bot/generate          │ Gửi câu hỏi AI     │
  ├──────┼─────────────────────────────────┼────────────────────┤
  │ 3    │ GET /api/bot/conversations/{id} │ Xem conversation   │
  ├──────┼─────────────────────────────────┼────────────────────┤
  │ 4    │ GET /api/bot/conversations      │ List conversations │
  └──────┴─────────────────────────────────┴────────────────────┘
  Test cases: Create → Send message → Get response → List history

  ---
  BẢNG PHÂN CẤP VAI TRÒ TEST
  ┌──────────────┬──────────────────┬───────────────────────────────────────────────────┐
  │   Vai trò    │      Quyền       │               Các luồng có thể test               │
  ├──────────────┼──────────────────┼───────────────────────────────────────────────────┤
  │ SuperAdmin   │ Tất cả           │ Onboarding review, Subscriptions, Brands, Reports │
  ├──────────────┼──────────────────┼───────────────────────────────────────────────────┤
  │ BrandAdmin   │ Brand scope      │ Hotels, Rooms, Subscriptions, Users (brand)       │
  ├──────────────┼──────────────────┼───────────────────────────────────────────────────┤
  │ HotelManager │ Hotel scope      │ Bookings, Rooms, Dashboard, Reports               │
  ├──────────────┼──────────────────┼───────────────────────────────────────────────────┤
  │ Receptionist │ Hotel operations │ Check-in/out, Bookings                            │
  ├──────────────┼──────────────────┼───────────────────────────────────────────────────┤
  │ Guest        │ Customer         │ Search, Book, Reviews, Wishlist, Profile          │
  └──────────────┴──────────────────┴───────────────────────────────────────────────────┘